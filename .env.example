# Backend environment example (DO NOT COMMIT real secrets)

# App
PORT=9000
NODE_ENV=development

# MongoDB — local example
MONGO_URL=mongodb://localhost:27017/admin-dashboard

# JWT secret (production: use GitHub Secrets / Secrets Manager)
JWT_SECRET=replace_with_strong_random_value

# Optional (email, third-party)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=